# Never add @tailwindcss/oxide-*-darwin-* or @next/swc-darwin-* as direct dependencies.
# tailwindcss / next install the correct optional binary per platform automatically.
package-lock=true
optional=true
